allprojects {
    apply plugin: 'java'
    sourceCompatibility = 1.8
    targetCompatibility = 1.8

    repositories {
        mavenCentral()
    }
}

jar {
    enabled = false
}

dependencies {
    compile project(':server')
    compile project(':demo')
}
